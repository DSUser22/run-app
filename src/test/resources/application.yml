server:
  error:
    include-message: always
    include-binding-errors: always

spring:
  datasource:
    password: dsuser22
    url: jdbc:postgresql://localhost:5432/run-db
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: havegoodrunnings@gmail.com
    password: ilxalufkoflbgjhy
    properties:
      mail:
        smtp:
          ssl:
            trust: "*"
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000

app:
  jwtSecret: dasha
  #  jwtExpirationMs: 3600000
  jwtExpirationMs: 60000
  #  jwtRefreshExpirationMs: 86400000
  jwtRefreshExpirationMs: 100000
  jwtCookie: "runyourbest"